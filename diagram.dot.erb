digraph G {
  rankdir=LR;
  <% Generator.find(:all).each do |generator| %>
    <% if generator.dependencies.size > 0 %>
      <% generator.dependencies.each do |dep_gen| %>
        <%= generator.name %> -> <%= dep_gen %>;
      <% end %>
    <% end %>
  <% end %>

}
